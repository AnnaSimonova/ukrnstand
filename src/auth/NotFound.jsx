import React from 'react';

const NotFound = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        fontSize: '5rem',
        color: 'red',
        height: '100vh',
        width: '100%',
      }}
    >
      Not Found
    </div>
  );
};

export default NotFound;
